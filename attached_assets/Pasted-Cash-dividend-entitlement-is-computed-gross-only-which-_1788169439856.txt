Cash dividend entitlement is computed gross only, which no custody operations
desk would accept. This is also a regression: the older
artifacts/api-server/src/lib/corporate-actions.ts required four terms for a
cash dividend (rate, recordDate, paymentDate, withholding) and carried a
"Confirm withholding guidance" task owned by Tax Operations. The v2 rewrite
replaced withholding with currency and dropped tax entirely.

It also creates a contradiction in the demo: the seeded Harbor break case
(evt-harbor-break) tells the analyst to "verify tax, rate, quantity" as the
investigation path for a GBP 2,000 settlement shortfall, while the engine has
no concept of tax.

Task: model withholding tax on cash dividends.

1. Add a withholding term to both seeded cash dividend events
   (evt-aurora-review, evt-harbor-break) with a real evidence quote and page
   number, and add withholding to their requiredTermKeys. The Aurora notice
   text already says "Withholding treatment remains subject to market
   documentation" on page 2, so give that event a low extraction confidence
   and make it a case where the analyst must supply the rate, which
   demonstrates the manual-correction-with-reason control.
2. Teach syncCalculationInput to coerce a withholding rate (accept both
   "15%" and "0.15" forms, store as a fraction).
3. In the Cash dividend branch of calculateEventImpacts, compute gross,
   withholding amount, and net. Round each through the BigInt helpers in
   src/lib/calculations.ts. Show all three in the formula string.
4. Reconciliation must compare against NET, not gross, since net is what
   actually settles.

REQUIRED, do not skip: new fields on the impact and reconciliation objects
will not reach the browser unless they are declared in the OpenAPI spec.
Outbound responses are validated against generated Zod schemas which strip
undeclared fields. So add the new fields to lib/api-spec/openapi.yaml
(Impact, Reconciliation, Term as needed), then run
  pnpm --filter @workspace/api-spec run codegen
and only then wire the frontend.

5. Update the workbench Impact stage
   (artifacts/corporate-actions-copilot/src/pages/EventWorkbench.tsx) to show
   gross, withholding and net per account.
6. Re-derive the Harbor break so its GBP 2,000 shortfall is now explainable
   in the system's own terms, and update its investigation steps to match.

Bump SEED_VERSION.

Verify: pnpm test passes, the Aurora dividend cannot be calculated until the
withholding term is validated, and the Impact stage shows gross, tax and net.